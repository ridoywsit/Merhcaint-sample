import { components } from 'generatedV2/server.type';

import { Address } from './util.type';

export type InvoiceLineItem = components['schemas']['InvoiceLine'];

export type Invoice = components['schemas']['Invoice'];

export type ExtractedInvoice = components['schemas']['ExtractedInvoice'];
